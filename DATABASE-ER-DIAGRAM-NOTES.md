# 数据库 ER 图笔记

一个用「档案柜」类比理解数据库实体关系图（ER Diagram）的笔记。

## 1. Entity（实体）= 专属档案袋

数据库不会把所有信息堆在一起，而是按类别分开存放，每一类对应数据库里的一张表（Table）：

- **患者档案袋**：只装患者的基本信息（姓名、生日、性别）。
- **住址档案袋**：只装地址的具体信息（省、市、街道、门牌号）。
- **处方档案袋**：只装药方信息（药名、剂量、开药时间）。

每一个「档案袋分类」，概念上就是一个 **Entity（实体）**。

## 2. Relationship（关系）= 档案袋之间的引用便签

如果把详细地址直接写在患者档案袋上，同住一家人共享同一地址时，改地址就要挨个改，容易出错。

所以患者档案不写完整地址，只贴一张便签：「详细住址请看：住址档案袋 #A007」。

- `#A007` 就是**外键（Foreign Key, FK）**，相当于一个跳转链接。
- 这种「通过便签把两个档案袋关联起来」的机制，就是 **Relationship（关系）**。

## 3. ER 图 = 线索墙

如果系统里有几百张表（比如 562 张），光看文字完全理不清谁和谁有关系。

ER 图就是把这些表做成方块贴在墙上，在有关系的表之间拉上连线。站在墙前一眼就能看清，例如：

> 【患者】拉出 3 条线，分别连着【住址】【病历】【紧急联系人】；【病历】又连着【医生】。

## 4. 连线上的基数符号（如 `}o--||`）

连线两端会标注「一对一 / 一对多 / 多对多」，即基数（Cardinality）：

- `}o`（分叉，像鸟爪）：代表「多个」。
- `||`（两条竖线，像栅栏）：代表「恰好 1 个」。

以 `patient }o--|| address` 为例：

- 左边患者是 `}`（多），右边地址是 `||`（恰好 1 个）。
- 翻译成人话：**多个患者可以引用同一个住址**（比如一家人住在一起）。

## 对实际开发的作用

ER 图相当于景区导览图。开发时想知道「查患者信息时怎么顺藤摸瓜拿到他的处方」，看这张线索墙就能直接找到路径，不用去翻后端几万行的建表代码。
