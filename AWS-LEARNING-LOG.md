# 学习问答笔记(AWS,面向前端/后端背景)

这份文件跟 `LEARNING-LOG.md`(Go 专用)是同类性质,但主题是 AWS——
起因是给 mykarte 的 S3/CloudFront 部署设计书做成本测算时,顺带把
HTTP 方法语义、CDN 原理、边缘计算这些概念一起理清楚了。按主题分类,
不按时间顺序,新的一轮问答直接往对应主题下面追加即可。

---

## 1. HTTP 方法语义:PUT / POST / DELETE,以及"幂等"到底是什么

### 三者是平级的语义,不是"谁是谁的特殊版本"

PUT、POST、DELETE 都是 HTTP 协议本身定义的动词,是平级关系,不存在
"PUT/DELETE 是特殊的 POST"这种说法:

- **POST**——"创建/提交",语义模糊,常用于新增资源
- **PUT**——"整体替换/上传",语义是"把这个资源整个设置成这个内容"
- **DELETE**——"删除"

S3 场景:PUT = 上传/覆盖某个 key 的文件,DELETE = 删除文件,POST 较少用。
AWS 按方法分别计费,是因为不同操作对 S3 内部资源消耗不同。

### 幂等(Idempotent)——名字听着绕,概念其实简单

数学定义:一个操作 f,如果 `f(f(x)) = f(x)`,就叫幂等——**执行一次和
执行多次,最终状态一样**(不是"过程中没有变化",而是"结果不会累加变化")。

类比:
- "把灯的开关**设置成**开"——幂等,按10次"设置为开",灯还是开着
- "按一下开关**切换**状态"——不幂等,按1次开、按2次变关、按3次又开

PUT 语义是"覆盖":
```
PUT /users/123  { name: "张三" }
```
调用1次或100次,`/users/123` 最终内容都是"张三"——幂等。

POST 语义通常是"追加":
```
POST /users  { name: "张三" }
```
调用1次创建1个用户,调用100次创建100个——每次结果不同,不幂等。

**判断标准不是词的长相,是操作语义"覆盖 vs 追加"**,这是 HTTP 协议的约定,
不是 AWS 自己发明的规则。

---

## 2. S3 成本构成

S3 收费主要看四块:

- **存储量**(GB/月)——按存储类型(Standard/IA 等)单价不同
- **请求次数**——GET/PUT/LIST 等,按次数计费,通常 GET 便宜、PUT 稍贵
- **数据传出流量**(Data Transfer Out)——通常是大头,尤其没有 CDN 挡在前面时
- **附加功能**——版本控制、跨区域复制,用到才要单独算

### 版本控制(Versioning)

开启后,同一个文件名每次被覆盖上传,旧版本不会真删除,会保留成历史版本,
可随时回滚(类比 Git 的历史提交)。代价:版本越多,存储空间越大,成本越高。

### 跨区域复制(Cross-Region Replication)

把一个区域(如东京 ap-northeast-1)的 bucket 内容自动同步一份到另一个
区域(如大阪或美国)。用途:容灾、就近访问加速。代价:多存一份数据 +
跨区域传输费,成本翻倍。如果业务只在日本运营、没有多活需求,通常不需要,
设计书里可以直接写"不采用"并说明理由。

---

## 3. CloudFront 为什么能省钱、Lambda@Edge、边缘节点

### CDN 省的不是"总流量",是省"从源站传出"这一段

不是"字节总数变少了",而是把"从 S3 传出"换成"从 CDN 边缘节点传出",
本质是两笔不同的账单:

- **没有 CDN**:用户每次请求直接打到 S3,S3 每次都收一次"数据传出"费
- **有 CDN**:第一次请求 CDN 从 S3 拉一次(这次 S3 传出算一次),之后
  内容缓存在边缘节点,后续大量重复请求从 CDN 节点直接返回,**不再经过
  S3**,S3 那边几乎不再产生传出流量

省的钱来自两处:① CDN 传出单价通常比 S3 直接传出便宜;② 大量重复请求
被缓存挡住,根本没有再产生"传出计费"这个动作。

### "CloudFront 和 S3 都在 AWS 内部,所以本质上免费"——方向对,但不完全

**CloudFront 回源到同区域的 S3,这段内部传输通常免费**(计入 AWS 内部
网络,不算"数据传出")。真正花钱的是"从 CloudFront 传给用户"这一段
(跨越 AWS 边界,一定收费,但比 S3 直接传出便宜)。

```
S3 → CloudFront(内部,免费)→ 用户(跨出AWS,收费,但比S3直接传出便宜)
```

### 边缘节点(Edge Location)是什么

CloudFront 在全球各地(东京、大阪、首尔、新加坡……)部署的服务器,离用户
最近的那些节点。用户请求被路由到物理最近的边缘节点,而不是绕地球去源站
(比如东京的 S3)请求,延迟更低。

### Lambda@Edge 是什么、什么时候需要

在 CloudFront 边缘节点上跑的一小段代码,能在"请求/响应经过 CDN 节点"这
个时间点做处理,比如:根据用户所在国家/设备类型返回不同内容、做简单鉴权、
AB 测试分流、改写 header。

**判断要不要用**:如果 CDN 只是单纯"缓存静态文件、加速传输",没有"边缘
拦截加工"的需求,就完全不需要,不用为了显得全面硬凑功能。

---

## 4. 边缘鉴权 vs 后端鉴权——一个重要的安全认知纠偏

**前端(浏览器里跑的 JS)本身是不可信的**:用户能打开 devtools 随便改
代码、伪造 token、跳过前端检查逻辑。前端鉴权检查只能做体验层面的引导
(没登录跳转登录页),不能作为真正的安全防线。

**真正的鉴权必须在服务器端做**,这是常识,不是 AWS 的规则。

CloudFront/Lambda@Edge 在这里的角色:**不是替代后端鉴权,是在后端鉴权
之前加一层"粗筛"**——完全没带 token 的请求(明显是恶意扫描/爬虫)可以在
边缘节点直接拒绝,不用让请求跑到源地区的后端服务器再被拒绝一次,省资源、
降延迟。

**结论**:主鉴权逻辑通常还在自己的后端/API 做,CloudFront 鉴权是可选的
性能优化项,不是必须的,也不是"一般都这样做"。如果后端鉴权已经跑得好好
的,这项可以直接在设计书里写"不采用,现有后端鉴权已足够"。

---

## 5. AB 测试分流 ≠ 负载均衡——目的完全不同

**负载均衡**:目的是把流量均匀分散到多台服务器,所有服务器跑**同一套
代码/内容**,用户看到的东西一样,纯粹为了扛并发、提高可用性。

**AB 测试分流**:目的是让不同用户看到**故意不同**的版本(A组/B组),做
对比实验(比如"按钮蓝色还是红色转化率更高")。分流依据不是"哪台服务器
不忙",而是"这个用户被随机分到了实验组还是对照组"。

版本 A、B 具体差异是什么,由产品/设计团队决定,不是技术层面自动产生的。
两者只是都涉及"把请求分配到不同地方"这个动作,容易被搞混,但是完全不同
层面的东西。

---

## 6. 成本测算方法论:怎么从"不懂 AWS"落地到一个具体数字

### 测算要回答的两个问题

设计书 §0/§2 通常要支撑:① 这个方案每月大概花多少钱;② 为什么选它而不是
别的方案。所以成本测算不是堆数字,是要支撑"采用理由"这条逻辑线。

### 增长缓冲(Growth Buffer)

预留余量。比如测算出"这个月大概 500 美元",但实际用户量/数据量可能涨得
比预期快,所以写"预计月成本约 500-650 美元(含 30% 增长缓冲)",避免测算
值刚好卡着预算导致后面超支,也给评审者一个"已考虑不确定性"的印象。

### 用 Cost Explorer 查真实现状,而不是凭空假设

**查具体服务的费用**:Billing and Cost Management → Cost Explorer →
右侧 Filters → Service,筛出 S3、CloudFront 等具体服务,能看到各自的
真实月费用趋势。

**查费用的构成明细**:把 Group by 从 "Service" 改成 "**Usage Type**",
能看到更细分类,比如 `TimedStorage-ByteHrs`(存储)、`Requests-Tier1`
(PUT 类请求)、`Requests-Tier2`(GET 类请求)、`DataTransfer-Out-Bytes`
(传出流量)——能判断"这笔钱贵在存储还是贵在传出流量",这个信息比"文件
类型是什么"对成本测算更直接有用。

**实测案例**(mykarte 真实数据,2026-07):S3 月费 $639.78,CloudFront
月费仅 $0.10——说明目前**几乎没有在用 CloudFront**,S3 的这笔钱基本
都是"直接对外传出"产生的。这个数字可以直接当作接入 CloudFront 后的
测算基准(baseline):假设缓存命中率能到 70-80%(静态资源通常容易做到),
大部分重复请求会被 CDN 挡住,S3 传出费用应显著下降,同时新增一笔更便宜的
CloudFront 传出费用。

**方法论总结**:先有真实基准数据,再谈百分比测算,不要凭空假设一个降幅。
