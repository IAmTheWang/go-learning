# Go 简史小记：TypeScript 编译器为什么改用 Go 重写

## 1. 时间线

- **6.x 及之前**：`tsc` 是 self-hosted 的——用 TypeScript 写编译器本身，编译成 JS 跑在 Node.js/V8 上。
- **2025-03**：微软（Anders Hejlsberg 主导）宣布原生重写项目 `typescript-go`，CLI 叫 `tsgo`，目标是用 Go 重写编译器 + 语言服务，换取约 **10 倍**的编译/类型检查速度提升。项目地址 `github.com/microsoft/typescript-go`，早期以 `@typescript/native-preview` npm 包形式发布 preview。
- **2026-07-08**：Go 版本正式发布为 **TypeScript 7.0**，不再是 preview。官方博客：`devblogs.microsoft.com/typescript/announcing-typescript-7-0`。GitHub 上有 `typescript/v7.0.2` 正式 release，附各平台原生二进制。

结论：TypeScript 6.x 的编译器是 TS 写的，7.0 的核心编译器是 Go 写的，目前两套代码处于并存/过渡状态。

## 2. 根本瓶颈不是"TS 语言性能差"，而是运行时（V8/Node）的结构性限制

- **JIT warmup 开销**：JS 是垃圾回收 + JIT 编译，长期运行才能跑到最优性能。但编译器/语言服务这种场景经常是频繁短时启动（比如 VSCode 里敲代码触发的语言服务），JIT 还没热起来任务就结束了。
- **单线程模型**：Node.js 是单线程事件循环，没法把类型检查这种可并行的工作真正分摊到多核 CPU 上；Worker 线程有序列化开销，不是真正共享内存的并行。

这跟"TypeScript 语言设计得好不好"无关，纯粹是运行时的物理限制。

## 3. 为什么最终选 Go，而不是 C / Java / C# / Rust

- **不选 C/C++**：代码库极其庞大复杂（几十万行经过多年打磨的类型推导逻辑），手动内存管理在这种规模下引入内存安全 bug 的风险太高。目标是"机械式"照搬现有逻辑而非重新设计架构，C 的心智负担会拖慢移植、增加风险。
- **不选 Rust**：内存安全，但所有权/借用检查器学习曲线陡峭。原 TS 编译器代码大量使用简单树状结构和"随便传引用"写法，硬套到 Rust 的所有权模型要大改架构，团队里 Rust 专家也不够多，会严重拖慢进度。
- **不选 Java/C#**（有意思的是 Anders Hejlsberg 本人是 C# 之父）：都跑在虚拟机上（JVM/CLR），同样有 JIT warmup 问题，跟 Node.js 的痛点类似，没有真正解决"启动慢"这个核心诉求。.NET 虽有 Native AOT，但 Go 在**跨平台交叉编译**上是真正的杀手锏——一次发布覆盖 linux-arm、loong64、riscv64、s390x 等一大堆冷门架构，且不需要目标平台工具链就能直接产出静态二进制，这种简单程度目前没有其他语言能做到。
- **选 Go 的关键优势**：
  1. 内存安全（GC），不需要 Rust 那种陡峭的所有权模型
  2. goroutine 并发模型天然契合"多核并行做类型检查"的需求，写法比手动加锁/async-await 简单直接
  3. 交叉编译能力一流，跨平台分发成本极低
  4. 迁移现有大型、树状结构为主的代码库时，改动幅度最小

## 4. 一句话总结

不是"TS 作为语言性能差"，而是"JS/TS 的运行时（V8/Node）在编译器这类工作负载下有结构性天花板"。选 Go 是在**内存安全、并行能力、跨平台分发、移植难度**这几个维度里找到的工程上最省事的平衡点——不是单纯的"理论性能最优解"（论峰值性能 Rust/C++ 可能更快，但移植成本团队扛不住）。
