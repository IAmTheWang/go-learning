package basics

// TODO: implement each function below.
// Run `go test ./01-basics/...` to check your work.

// Sum returns a + b.
func Sum(a, b int) int {
	return 0
}

// IsEven reports whether n is even.
func IsEven(n int) bool {
	return false
}

// FizzBuzz returns "Fizz" if n is divisible by 3, "Buzz" if divisible by 5,
// "FizzBuzz" if divisible by both, otherwise n itself as a string.
// Hint: strconv.Itoa(n) converts an int to a string.
func FizzBuzz(n int) string {
	return ""
}

// Grade converts a numeric score to a letter grade using a switch statement:
// >=90 "A", >=80 "B", >=70 "C", >=60 "D", otherwise "F".
func Grade(score int) string {
	return ""
}
