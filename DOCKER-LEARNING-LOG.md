# 学习问答笔记(Docker,面向前端/后端背景)

这份文件跟 `LEARNING-LOG.md`(Go 专用)、`AWS-LEARNING-LOG.md` 是同类性质,
主题是 Docker——起因是打开 Docker Desktop 看 Images/Containers 列表时,
顺带把镜像、容器的关系理清楚了。按主题分类,不按时间顺序,新的一轮问答
直接往对应主题下面追加即可。

---

## 1. npm start(本地跑)vs docker(容器里跑)

**npm/pnpm/yarn start(或 dev)**
- 直接用宿主机上安装的 Node.js 运行项目代码
- 依赖(`node_modules`)、Node 版本、系统库都用本机现成的环境
- 启动快、热更新灵敏,调试方便
- 缺点:"在我电脑上能跑"问题——同事的 Node 版本、OS、全局依赖不一样,行为可能不一致

**docker run / docker compose up**
- 先把代码 + Node 运行时 + 系统依赖打包成一个**镜像(image)**,再在隔离的
  **容器(container)**里跑
- 容器内部环境固定、可复现,不依赖宿主机装了什么
- 好处:环境一致性(开发/测试/生产用同一个镜像)、隔离性、更接近真实部署环境
- 代价:启动稍慢、热更新需要额外配置(卷挂载)、调试相对麻烦

**类比**:`npm start` 像直接在自己电脑上跑脚本;docker 像给这个脚本套了一个
"随身携带的迷你电脑"(自带 OS、依赖、配置),不管拿到哪台机器上跑结果都一样。

---

## 2. Docker Desktop:Images 和 Containers 到底是什么

用面向对象的类比最好理解:

| 概念 | 类比 | 说明 |
|---|---|---|
| **Image(镜像)** | "类(Class)" / 安装包 / 快照 | 一个**只读的模板**,打包了运行代码所需的一切:代码、运行时(比如 Node)、依赖库、系统文件、启动命令等 |
| **Container(容器)** | "实例(Instance)" | 由某个 Image **启动/实例化**出来的、正在运行(或曾经运行过)的进程。同一个 Image 可以启动出多个 Container(比如同一个 postgres 镜像跑两个不同端口的数据库实例) |

对应到 Docker Desktop 左侧菜单:

- **Images 页面**:列出本地下载/构建好、但不一定在运行的所有镜像模板。能看到
  大小、创建时间、Image ID。操作是"删除镜像""基于镜像启动一个容器"(▶️ 按钮)
- **Containers 页面**:列出基于这些镜像**实际启动出来的运行实例**。能看到:
  哪个在跑(绿色)、哪个停了、占用什么端口、CPU/内存使用情况、日志(logs)、
  进容器执行命令(exec)

**对应关系**:
- `docker pull postgres` / `docker build .` → 生产出一个 **Image**(下载/打包安装包)
- `docker run postgres` → 用这个 Image **启动一个 Container**(相当于 `npm start` 跑起来的那个进程)

**一句话总结**:Image 是"菜谱+食材包",Container 是"照着菜谱做出来、正在
桌上的那盘菜"。同一份菜谱能做很多盘菜(多个容器),但菜谱本身不会因为做了
一盘菜而被吃掉或改变。

---

## 3. 为什么前端可以热更新(HMR)

热更新全称 **HMR(Hot Module Replacement,模块热替换)**。核心思路:**不刷新
整个页面,只替换发生变化的那一小块代码**,同时尽量保住当前的运行状态(比如
React 组件的 state、Vue 的响应式数据)。

### 实现链路

1. **文件监听**:开发服务器(vite / webpack-dev-server)内部有一个**文件
   监听器(file watcher)**,监控项目源文件。"file watcher" 是通用叫法,不是
   某个工具专属的——底层复用同一个库 `chokidar`,它把不同操作系统的文件
   系统事件 API(macOS `FSEvents`、Linux `inotify`、Windows
   `ReadDirectoryChangesW`)包装成统一接口
2. **通信**:页面加载时,开发服务器往 HTML 里注入一段客户端脚本,跟开发
   服务器建立 **WebSocket 长连接**
3. **推送**:文件变化 → 服务器重新编译**这一个模块**(不是整个项目)→
   通过 WebSocket 通知浏览器"这个模块变了,新代码在这"
4. **替换**:浏览器用 `import()` 拉取新模块代码,替换掉内存里旧模块的引用,
   触发框架自己的"接受更新"逻辑(React Fast Refresh 保留 state 重新渲染;
   Vue 重新执行 setup 但保留响应式状态)
5. **CSS 更新**更简单:直接把 `<style>` 标签内容整个换掉,因为 CSS 没有
   "状态"要保留

### 为什么编译型语言(比如 Go)做不到这么彻底

Go 编译出的是一个**静态二进制**,整个程序是不可分割的进程——没有"模块"这种
运行时可替换的概念。所以 Go 的热更新工具(`air`、`fresh`)本质是:监听文件
变化 → **重新编译整个程序** → **杀掉旧进程、启动新进程**。这是"重启"不是
真正的"热替换",内存里的状态(比如内存缓存)都会丢失。

**类比**:前端热更新像给运行中的程序"活体换零件"(只换坏的那颗螺丝,机器
不停);Go 这类编译型语言更像"整机重启"——因为没有天然支持运行时替换某一
部分代码的模块系统。

---

## 4. Vite 和 Webpack 是什么关系——竞争对手,不是搭档

它们不是"形影不离",而是**做同一件事的两个替代方案**,大部分项目只会
二选一使用。

### 解决的是同一个问题

前端代码写成一堆 `.js`/`.ts`/`.vue`/`.css` 文件,浏览器不能直接高效运行
这些原始文件(老浏览器不支持 ES Module、文件太多请求太多),需要工具把它们
**打包/处理**成浏览器能高效加载的产物。

### 对比

| | Webpack | Vite |
|---|---|---|
| 出现时间 | 2012年左右,历史久、生态最成熟 | 2020年,尤雨溪(Vue 作者)做的,后起之秀 |
| 开发模式思路 | 无论开发还是生产,都要先把所有模块**打包**成一个或几个大文件 | 开发模式下**不打包**,直接让浏览器用原生 `import` 按需加载单个文件;只有生产构建时才打包(用另一个工具 Rollup) |
| 结果 | 项目越大,启动/热更新越慢(要重新算依赖图、重新打包) | 启动几乎瞬间,热更新极快,不用整体重新打包 |
| 生产构建 | 自己打包 | 底层调用 **Rollup** 打包(自己不重复造轮子) |

### 关系

1. **技术演进关系**:Vite 是为了解决 Webpack 开发体验慢的问题而诞生的
   "下一代"方案,某种程度是 Webpack 的"继任者"
2. **老项目 vs 新项目**:老项目大概率还在用 Webpack(Vue CLI / Create React
   App 脚手架);新项目(Vue3、新 React 项目)默认脚手架已经换成 Vite
3. 有时同一篇文章/教程会把两者放一起对比,容易让人误以为它们经常搭配出现

**一句话总结**:它们是同一个岗位的两代打字员——Webpack 是老资格全能选手
(功能多、配置灵活但慢),Vite 是新人(开发体验飞快,生产打包借用 Rollup
这个"外援"),项目里通常**二选一**,不会同时用两个来打包同一套东西。
